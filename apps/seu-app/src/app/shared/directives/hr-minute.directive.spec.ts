import { HrMinuteDirective } from './hr-minute.directive';

describe('HrMinuteDirective', () => {
  it('should create an instance', () => {
    const directive = new HrMinuteDirective();
    expect(directive).toBeTruthy();
  });
});
