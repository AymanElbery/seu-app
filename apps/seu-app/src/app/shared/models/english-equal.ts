export interface EnglishEqual {
    tests: Array<{test: number, score: number, date: string, number: number}>;
    attachment: string;
    attachment2: string;
    ENG_TESTS: Array<{TEST_TITLE, TEST_PK, test: number, score: number}>;
    notes: any;
    crse_transfer_grades: any;
}
